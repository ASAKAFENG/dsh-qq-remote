# dsh-qq-remote 自装配 patch（dsh plugin 安装 / include.refresh 时生效）
# inject 声明：等待 webServer / tools 服务就绪后再 apply，否则面板路由不会注册
- insert:
    - id: dsh-qq-remote
      name: '@dsh-external/dsh-qq-remote'
      inject: [webServer, tools]
      config: {}
